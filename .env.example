# tlusr-scraper-playground — local env (scratch). Copy to `.env.local`.
# Playground only; no real secrets belong here.

CONFIGS_DIR=./configs   # where site configs are read from
DATA_DIR=./data         # scratch output directory
